# CMakeList.txt : CMake project for Time converter v2, include source and define
# project specific logic here.
#
cmake_minimum_required (VERSION 3.8)

# Add the source for the project's executable.
add_executable (Time_Converter "time_converter.cpp")